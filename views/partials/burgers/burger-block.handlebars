<div class="col s12 m12 l12">
  <div class="card">
    <div class="card-content">
      <span class="card-title truncate">{{burger_name}}</span>
      {{#if devoured}}{{else}}        
        <div class="card-action">
          <a class="devour-burger red-text lighten-1" data-id="{{id}}">Eat It</a>
        </div>
      {{/if}}
    </div>
  </div>
</div>
